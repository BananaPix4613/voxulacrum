mod camera;
mod cloud_shadow;
mod meshing;
mod rendering;
mod simulation;
mod world;

fn main() {
    env_logger::init();
    log::info!("Voxulacrum engine starting...");
    println!("Dev environment ready. Phase 1 setup complete.");
}
