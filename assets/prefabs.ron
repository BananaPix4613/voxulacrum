// Scatter prefab registry - procedural prop definitions for Tier 2/3 foliage.
// `id` is the stable PrefabId (contiguous range from 0); file order is not
// authoritative. Keep in sync with PrefabRegistry::load_initial() (a unit test
// locks the two together).
(
    prefabs: [
        (id: 0, id_name: "grass_tuft", shape: GrassTuft, color: (0.30, 0.55, 0.20), scale: 0.65),
        (id: 1, id_name: "bush",       shape: Bush,      color: (0.20, 0.42, 0.16), scale: 0.85),
        (id: 2, id_name: "rock",       shape: Rock,      color: (0.50, 0.50, 0.53), scale: 0.75),
    ],
)